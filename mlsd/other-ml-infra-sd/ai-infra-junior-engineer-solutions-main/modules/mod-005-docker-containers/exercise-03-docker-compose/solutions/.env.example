# Database Configuration
POSTGRES_DB=mldb
POSTGRES_USER=mluser
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://mluser:changeme@postgres:5432/mldb

# Redis Configuration
REDIS_URL=redis://redis:6379/0
REDIS_PASSWORD=changeme

# MLflow Configuration
MLFLOW_TRACKING_URI=http://mlflow:5000
BACKEND_STORE_URI=postgresql://mluser:changeme@postgres:5432/mlflowdb
ARTIFACT_ROOT=/mlflow/artifacts

# MinIO (S3) Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme
MINIO_ENDPOINT=http://minio:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=changeme

# Application Configuration
MODEL_PATH=/models
ENVIRONMENT=production
LOG_LEVEL=INFO
WORKERS=4

# Monitoring Configuration
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme

# Jupyter Configuration
JUPYTER_TOKEN=
JUPYTER_PASSWORD=

# Security
SECRET_KEY=changeme-generate-random-key
JWT_SECRET=changeme-generate-random-key
