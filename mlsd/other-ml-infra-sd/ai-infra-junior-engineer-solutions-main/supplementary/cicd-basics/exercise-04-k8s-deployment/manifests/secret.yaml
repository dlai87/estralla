# This is a TEMPLATE file. Do NOT commit actual secrets to git.
# Use one of these approaches for production:
# 1. kubectl create secret generic ml-api-secrets --from-literal=api-key=your-key
# 2. Use Sealed Secrets
# 3. Use External Secrets Operator
# 4. Use HashiCorp Vault

apiVersion: v1
kind: Secret
metadata:
  name: ml-api-secrets
  namespace: ml-api
  labels:
    app: ml-api
type: Opaque
stringData:
  # Replace with actual values or use secret management solution
  api-key: "REPLACE_WITH_ACTUAL_API_KEY"
  db-password: "REPLACE_WITH_ACTUAL_DB_PASSWORD"
  redis-password: "REPLACE_WITH_ACTUAL_REDIS_PASSWORD"

  # Example for base64-encoded secrets (not recommended for git)
  # data:
  #   api-key: <base64-encoded-value>
