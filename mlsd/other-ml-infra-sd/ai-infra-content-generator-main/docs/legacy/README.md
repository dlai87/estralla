# Legacy Documents

These documents are retained as historical reference. They describe the earlier
template-oriented content framework and may mention retired practices such as
word-count targets. The active operational contract is the root `README.md` and
`docs/RUNBOOK.md`.
